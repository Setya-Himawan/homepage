<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WebDev</title>
  </head>
  <body>
    <h1>Hallo nama saya riski</h1>

    <ul>
      <% users.forEach(function(user) { %>
      <li><%= user.username %></li>
      <!-- Menampilkan username dari setiap user -->
      <% }); %>
    </ul>
  </body>
</html>
